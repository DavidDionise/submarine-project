# Hardware control (ESC motor + steering servos via pigpio daemon)
gpiozero==1.6.2
pigpio==1.78

# Compass (core/hardware/compass/compass_publisher.py, LIS3MDL sensor)
# NOTE: these two were never actually listed in the old requirements.txt,
# even though compass_publisher.py imports them directly. Unpinned since
# there's no prior known-good version to go on -- pin once confirmed working.
Adafruit-Blinka
adafruit-circuitpython-lis3mdl

# ws_client
websocket-client==1.4.2
rel==0.4.7

# sse_client
# NOTE: also missing from the old file despite being imported directly.
sseclient-py

# gotify_client
# NOTE: also missing from the old file despite being imported directly.
gotify

# --- Optional: only needed if you still use the deprecated compass code
# (core/hardware/deprecated_compass/*.py -- calibrate.py, compass_publisher_01/02.py).
# Not imported by any of the active entry points (cl_runner, ws_client,
# sse_client, gotify_client), so left commented out. Uncomment if you use it.
# smbus2==0.4.2
